# Plan: Work Products UI Section in IssueProperties

**Branch:** master  
**Session:** 001  
**Date:** 2026-05-07  
**Status:** REVIEWED & APPROVED (auto)

---

## Context

After codebase scan, the reality is:
- **DB, server routes, shared types, UI API client** — all 100% implemented
- **Missing**: A UI section in `IssueProperties.tsx` to display `issue.workProducts`
- The issue detail route already returns `workProducts` inline with the issue object
- `Issue` type already has `workProducts?: IssueWorkProduct[]`

**Scope reduction from spec**: This is a single-file UI addition. No DB changes, no route changes.

---

## Steps

### Step 1 — Add Work Products section to `IssueProperties.tsx`

**File:** `ui/src/components/IssueProperties.tsx`

**What:** Add a "Work Products" section that:
1. Reads `issue.workProducts ?? []` from the issue prop (already populated via issue detail route)
2. Only renders when there are work products (no empty state — clutter-free sidebar)
3. Renders each product with:
   - Type icon: `pull_request` → GitPullRequest, `branch` → GitBranch, `preview_url` → Globe, `artifact`/`document` → FileText, default → Package
   - Title as clickable link (when `url` is present)
   - Status badge for non-active states (merged, closed, draft, etc.)
   - Agent attribution (via `createdByRunId` → show "by agent" when present)

**Where:** Insert after the workspace section (around line 1315, before the `<Separator />` before "Created by")

**Imports to add:**
- `IssueWorkProduct` from `@paperclipai/shared` (already imported via `Issue` type — need explicit import)
- `GitPullRequest, Globe, Package, FileText` from `lucide-react` (GitBranch already imported)

### Step 2 — Verify typecheck passes

```
pnpm -r typecheck
```

### Step 3 — Run tests

```
pnpm test:run
```

---

## Acceptance Criteria

- [ ] Work Products section visible in Issue detail sidebar when `workProducts` is non-empty
- [ ] Each product shows type icon + title
- [ ] `url` present → title is a clickable `<a>` with `target="_blank" rel="noopener noreferrer"`
- [ ] Section is hidden when `workProducts` is empty or undefined
- [ ] `pnpm -r typecheck` passes
- [ ] `pnpm test:run` passes

---

## Out of Scope (Phase 1)

- Board manually adding work products via UI (API exists, UI form deferred)
- Board deleting work products via UI (API exists, deferred)
- Status filtering or sorting
